# This is a Databricks asset bundle definition for bundle_insur.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle_insur
  uuid: 354743b1-2455-47fb-80a1-41259bfef299

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0efcfc5e-695c.cloud.databricks.com
    variables:
      catalog: insurance_dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-0efcfc5e-695c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/jeevanprabhakaran19@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jeevanprabhakaran19@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: insurance_dev
      schema: prod
    permissions:
      - user_name: jeevanprabhakaran19@gmail.com
        level: CAN_MANAGE
